<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.dykj.domain.notification.repository.HoldingMapper">

    <select id="selectAllHoldings" resultType="com.project.dykj.domain.stock.vo.Holding">
        SELECT 
            USER_ID as userId, 
            STOCK_ID as stockId, 
            TOTAL_PRICE as totalPrice, 
            TOTAL_COUNT as totalCount
        FROM HOLDINGS
    </select>

    <select id="selectHoldingByUserId" resultType="com.project.dykj.domain.stock.vo.Holding" parameterType="string">
        SELECT 
            USER_ID as userId, 
            STOCK_ID as stockId, 
            TOTAL_PRICE as totalPrice, 
            TOTAL_COUNT as totalCount
        FROM HOLDINGS
        WHERE USER_ID = #{userId}
    </select>

</mapper>