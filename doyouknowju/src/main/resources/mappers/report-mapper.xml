<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.dykj.domain.report.mapper.ReportMapper">

    <insert id="insertReport" parameterType="com.project.dykj.domain.report.vo.ReportVo">
        INSERT INTO REPORTS (
            REPORT_ID, 
            REPORT_TYPE, 
            CONTENT_ID, 
            REPORTER_ID, 
            TARGET_ID, 
            REPORT_REASON,
            REPORT_DATE,
            STATUS
        ) VALUES (
            SEQ_REPORT_ID.NEXTVAL, 
            #{reportType}, 
            #{contentId}, 
            #{reporterId}, 
            #{targetId}, 
            #{reportReason},
            SYSDATE,
            'PENDING'
        )
    </insert>
    
    <select id="selectReportList" resultType="com.project.dykj.domain.report.vo.ReportVo">
    	SELECT
	    	REPORT_ID,
	    	REPORT_TYPE,
	    	CONTENT_ID,
	    	REPORTER_ID,
	    	TARGET_ID,
	    	REPORT_REASON,
	    	REPORT_DATE,
	    	STATUS
	    FROM REPORTS
	    ORDER BY REPORT_DATE DESC
    </select>
    
    <select id="selectReportListPaged" resultType="com.project.dykj.domain.report.vo.ReportVo">
        SELECT * FROM (
            SELECT ROWNUM AS RN, A.* FROM (
                SELECT
                    REPORT_ID,
                    REPORT_TYPE,
                    CONTENT_ID,
                    REPORTER_ID,
                    TARGET_ID,
                    REPORT_REASON,
                    REPORT_DATE,
                    STATUS
                FROM REPORTS
                <where>
                    <if test="status != null and status != ''">
                        AND STATUS = #{status}
                    </if>
                </where>
                ORDER BY REPORT_DATE DESC
            ) A
            WHERE ROWNUM &lt;= #{offset} + #{size}
        )
        WHERE RN &gt; #{offset}
    </select>

    <select id="selectTotalReportCount" resultType="_int">
        SELECT COUNT(*) FROM REPORTS
        <where>
            <if test="status != null and status != ''">
                AND STATUS = #{status}
            </if>
        </where>
    </select>
    
    <!-- 단건 신고 조회 -->
    <select id="selectReportById" resultType="com.project.dykj.domain.report.vo.ReportVo">
        SELECT
            REPORT_ID,
            REPORT_TYPE,
            CONTENT_ID,
            REPORTER_ID,
            TARGET_ID,
            REPORT_REASON,
            REPORT_DATE,
            STATUS
        FROM REPORTS
        WHERE REPORT_ID = #{reportId}
    </select>

    <!-- 신고 상태 변경 -->
    <update id="updateReportStatus">
        UPDATE REPORTS
        SET STATUS = #{status}
        WHERE REPORT_ID = #{reportId}
    </update>

</mapper>